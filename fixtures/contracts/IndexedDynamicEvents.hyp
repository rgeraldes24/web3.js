// SPDX-License-Identifier: GNU
pragma hyperion >=0.0;

contract IndexedDynamicEvents {
	event IndexedBytes(bytes indexed value);

	function fire(bytes calldata value) public {
		emit IndexedBytes(value);
	}
}
